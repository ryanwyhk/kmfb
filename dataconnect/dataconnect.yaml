specVersion: "v1"
serviceId: "firebase"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "firebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
